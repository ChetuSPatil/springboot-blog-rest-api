server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/myblog
    username: postgres
    password: postgres

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        #dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
  security:
    user:
      name: chetu
      password: chinnu
      roles: ADMIN